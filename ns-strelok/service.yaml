apiVersion: v1
kind: Service
metadata:
  labels:
    app: strelok-app
  name: strelok-app
  namespace: strelok
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: strelok-app
  type: LoadBalancer
